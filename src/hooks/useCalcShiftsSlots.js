// import { useState } from 'react';
// import TasksNotFilled from '../components/TasksNotFilled';

// const useCalcShiftsSlots = (task, slotType) => {
//   const [shiftData, setShiftData] = useState({
//     slots: null,
//     filledSlots: null,
//   });

//   const slots = task.shifts
//     .map((shift) => shift[slotType])
//     .reduce((acc, cur) => acc + cur, 0);
//   console.log(slots);

//   //   setShiftData({ slots: null, filledSlots: null });

//   return;
// };

// export default useCalcShiftsSlots;
