// import { closedTasks } from '../data/data';
// import TaskItemContainer from '../UI/TaskItemContainer';

// const Closed = () => {
//   //console.log(assignmentNeededTasks);
//   const tasks = closedTasks.map((task) => <TaskItemContainer task={task} />);

//   return <div>{tasks}</div>;
// };

// export default Closed;
